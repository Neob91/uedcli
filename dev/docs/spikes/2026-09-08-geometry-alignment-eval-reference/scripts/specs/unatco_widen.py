"""Task: widen Manderley's office (UNATCO HQ) -- see ../spec_format.py for
the entry-kind vocabulary (update/anchor/create/delete) this file uses.
"""

TASK = dict(
 id="unatco_widen", level="unatco",
 title="Task 1 — Widen the office",
 req="&ldquo;Manderley&rsquo;s office is too narrow. Widen it eastward by 48 units &mdash; move its east wall out.&rdquo;",
 base_trunk="unatco_gt",
 frame="59,-360,200,620,140,500",
 photo_camera=dict(at=[300,-112,330], pitch=0),
 before=dict(
   quad="before_quad",
   note="Built from TWO separate wall volumes that share one east wall (the south half is Brush418, the north half is Brush420) — a detail invisible to a player, but important for widening the wall correctly.",
   photos=[f"pan_before_{i}" for i in range(8)]),
 entries=[
   dict(kind="update", actor="Brush418", target="corners", at=[[448,-288,416],[448,64,416],[448,64,240],[448,-288,240]], delta=[48,0,0],
        what="south wall of Manderley's office; the task's own east-widen target"),
   dict(kind="update", actor="Brush420", target="corners", at=[[448,64,416],[448,304,416],[448,304,240],[448,64,240]], delta=[48,0,0],
        what="north wall of Manderley's office, shares the east wall with Brush418 at the y=64 seam -- must widen the same amount or the wall steps"),

   dict(kind="anchor", actor="Brush670", to="Brush418", what="switch recess, cut into the south wall"),
   dict(kind="anchor", actor="Brush161", to="Brush418", what="south wall trim board"),
   dict(kind="anchor", actor="Brush132", to="Brush418", what="south wall trim board"),
   dict(kind="anchor", actor="Brush203", to="Brush418", what="south wall trim board"),
   dict(kind="anchor", actor="Brush74", to="Brush418", what="south wall trim board"),
   dict(kind="anchor", actor="Brush152", to="Brush418", what="south wall fixture"),
   dict(kind="anchor", actor="Switch6", to="Brush418", what="the wall light switch itself, mounted in Brush670's recess"),
   dict(kind="anchor", actor="Brush869", to="Brush420", what="decorative pilaster on the north wall"),
   dict(kind="anchor", actor="Light14", to="Brush420", what="the pilaster's own light"),
   dict(kind="anchor", actor="Brush766", to="Brush420", what="wall cavity holding the trophy shelf + hidden safe, cut into the north wall"),
   dict(kind="anchor", actor="Brush364", to="Brush420", what="the safe's own interior geometry inside the cavity"),
   dict(kind="anchor", actor="Brush873", to="Brush420", what="the safe's own back panel inside the cavity -- moving the cavity without this orphans it mid-room (the original bug this eval caught)"),
   dict(kind="anchor", actor="Brush592", to="Brush420", what="the display shelf inside the cavity"),
   dict(kind="anchor", actor="Light149", to="Brush420", what="the shelf's own light"),
   dict(kind="anchor", actor="Vase3", to="Brush420", what="display item on the shelf"),
   dict(kind="anchor", actor="Vase4", to="Brush420", what="display item on the shelf"),
   dict(kind="anchor", actor="BookClosed1", to="Brush420", what="display item on the shelf"),
   dict(kind="anchor", actor="NanoKey0", to="Brush420", what="display item on the shelf"),
   dict(kind="anchor", actor="WeaponModRecoil0", to="Brush420", what="display item on the shelf"),
   dict(kind="anchor", actor="FlagPole3", to="Brush420", what="ceremonial flagpole anchored to the north wall's corner (~48u off two adjacent walls, not flush-mounted)"),
   dict(kind="anchor", actor="FlagPole4", to="Brush418", what="ceremonial flagpole anchored to the south wall's corner"),
   dict(kind="anchor", actor="Brush663", to="Brush418", what="the display niche -- a genuinely separate small room built against the south wall, must stay flush against it"),
   dict(kind="anchor", actor="Brush1", to="Brush418", what="niche interior detail brush"),
   dict(kind="anchor", actor="Brush138", to="Brush418", what="niche interior detail brush"),
   dict(kind="anchor", actor="Brush140", to="Brush418", what="niche interior detail brush"),
   dict(kind="anchor", actor="Brush148", to="Brush418", what="niche interior detail brush"),
   dict(kind="anchor", actor="Brush164", to="Brush418", what="niche interior detail brush"),
   dict(kind="anchor", actor="Brush168", to="Brush418", what="niche interior detail brush"),
   dict(kind="anchor", actor="Brush1158", to="Brush418", what="niche interior detail brush"),
   dict(kind="anchor", actor="Brush1550", to="Brush418", what="niche interior detail brush"),
   dict(kind="anchor", actor="Brush1551", to="Brush418", what="niche interior detail brush"),
   dict(kind="anchor", actor="OrdersTrigger5", to="Brush418", what="scripted trigger inside the niche"),
   dict(kind="anchor", actor="Light6", to="Brush418", what="the niche's own light"),
 ],
 # scenario grouping for the human-readable page -- display only, does not affect grading
 scenarios={
   "wall": dict(title="East wall (both halves of the room)", view="top",
     note="The office is built from two wall volumes sharing one wall. Both must move out together, or the wall gets a step in it.",
     members=["Brush418","Brush420"]),
   "fixtures": dict(title="Wall-mounted fixtures (switch, trim, pilaster)", view="top",
     note="The wall's own hardware is split across both wall volumes: the switch/recess/most trims sit on the south half (Brush418), the pilaster and its light sit on the north half (Brush420). Each moves with its OWN wall, not a single shared delta.",
     members=["Brush670","Brush161","Brush132","Brush203","Brush74","Brush152","Switch6","Brush869","Light14"]),
   "safe": dict(title="Trophy shelf with a hidden wall safe", view="top",
     note="A wall-mounted display shelf has a real hidden safe built into its back, entirely on the north wall volume (Brush420). The whole unit and its contents must move together with it.",
     members=["Brush766","Brush364","Brush873","Brush592","Light149","Vase3","Vase4","BookClosed1","NanoKey0","WeaponModRecoil0"],
     extra_photos=[("photo_safe_before","Before: the trophy shelf — a vase, a polished rock, a closed book, and a nanokey on display."),
                   ("photo_safe_fixed","Correct: the whole shelf unit, including the hidden safe built into its back, moved out together with the wall."),
                   ("photo_safe_side","Side view showing the shelf is a real wooden cabinet built into the wall, with actual depth."),
                   ("photo_safe_behind","The space directly behind the safe: a sealed, empty dead-end — nothing else back there.")]),
   "flags": dict(title="Corner flagpoles", view="top",
     note="Two flagpoles stand in opposite corners of the room, anchored to DIFFERENT wall volumes: FlagPole3 to the north wall (Brush420), FlagPole4 to the south wall (Brush418). Each must move with its own corner's wall.",
     members=["FlagPole3","FlagPole4"],
     extra_photos=[("photo_flags_after","Photo confirming a flag sits flush back in its corner after the move.")]),
   "niche": dict(title="Display niche (a separate small room)", view="top",
     note="This niche is a genuinely separate small room built against the south wall volume (Brush418), not part of the wall itself. It must move so it stays flush against it.",
     members=["Brush663","Brush1","Brush138","Brush140","Brush148","Brush164","Brush168","Brush1158","Brush1550","Brush1551","OrdersTrigger5","Light6"]),
 })
