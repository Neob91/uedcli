+++
priority = "p3"
kind = "implement"
summary = "Stash/prefab lineage via a real git merge edge"
+++

# Stash/prefab lineage via a real git merge edge

— record a promoted prefab's source commit/branch as a second parent. *(Reframed 2026-07-18: "source session" → source commit; sessions removed by the git-native migration.)*
