+++
priority = "p3"
kind = "implement"
summary = "Unify `stash apply` + `prefab apply`"
+++

# Unify `stash apply` + `prefab apply`

into one resolver; v1 keeps them separate (shared `_place_actors`).
