+++
priority = "p?"
kind = "implement"
summary = "Remove the addressable A1-style coordinate grid gutter from level actor preview."
+++

# Remove numbering grid from level actor preview

`preview.py`'s "addressable coordinate grid" (column letters + row numbers gutter, `_draw_grid_gutter`
et al.) should be removed from level actor preview output. Note the section is marked "Owner-ruled
2026-08-02, LOCKED" — that ruling is superseded by this request.
