+++
priority = "p3"
kind = "chore"
summary = "done.md HYBRID entry went stale after the on-face port` — the \"HYBRID per-brush label tint + legend + `--focus`\" `board/done/` entry still describes the leader/arro"
+++

# done.md HYBRID entry went stale after the on-face port` — the "HYBRID per-brush label tint + legend + `--focus`" `board/done/` entry still describes the leader/arro

done.md HYBRID entry went stale after the on-face port` — the "HYBRID per-brush label
tint + legend + `--focus`" `board/done/` entry still describes the leader/arrow/box poly-label path and the
`_pale` label-box wash + black digits, all removed when on-face labeling became the sole renderer
(commit `9dfb1ee44`) and `_pale` was dropped (`8a557f1a1`). The durable docs were reconciled; only this
historical `board/done/` tail entry is stale. Rewrite or drop it. Noted during the `--split` build.
