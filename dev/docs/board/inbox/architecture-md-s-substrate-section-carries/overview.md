+++
priority = "p2"
kind = "chore"
summary = "`architecture.md`'s Substrate section carries a deferred note whose symbols are deleted"
+++

# `architecture.md`'s Substrate section carries a deferred note whose symbols are deleted

It still ends with "*Deferred follow-up: the stub-build + texture sync mounts are
sourced from `substrate_code_dirs`/`install_system_root`, NOT yet from the composed config*" —
un-deferred 2026-07-14 17:40, and both symbols are gone from `uedcli/`. *(containers drafter.)*
