+++
priority = "p?"
kind = "unknown"
summary = "FakeBackdrop identical"
+++

# FakeBackdrop identical

Exactly 1 FakeBackdrop surf (#4, flags `0x00400080`) in BOTH maps;
native DOES render the starfield where the backdrop is in view. Backdrop pipeline is fine.
