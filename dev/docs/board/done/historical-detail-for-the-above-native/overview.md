+++
priority = "p1"
kind = "unknown"
summary = "Superseded, folded into `native-light-apply-bake-where-it-stands-and`. Old-wave (pre-2026-08-14) historical detail note. Superseded — the native from-scratch `.dx` game-load path this describes was since replaced by `assemble_unbuilt` + `MAP LOAD` (see `editor-free-native-world-bsp-map-assembly`)."
+++

# (historical detail for the above) native from-scratch `.dx` did NOT game-load: engine could not instantiate the ULevel (`Failed to find object 'Level None.MyLevel'`)

Superseded, folded into `native-light-apply-bake-where-it-stands-and`.

Old-wave (pre-2026-08-14) historical detail note. Superseded — the native from-scratch `.dx` game-load path this describes was since replaced by `assemble_unbuilt` + `MAP LOAD` (see `editor-free-native-world-bsp-map-assembly`).
