+++
priority = "p2"
kind = "debug"
summary = "Superseded, folded into `native-light-apply-bake-where-it-stands-and`. Superseded by the fuller, later re-measurement in `breadth-geometry-re-check-across-11-og-levels-2`, which explicitly notes this item's own numbers drifted (expected, from commits landing between the two measurements)."
+++

# Geometry re-check on 4 more OG levels: 0/4 exact, all slightly over-built

Superseded, folded into `native-light-apply-bake-where-it-stands-and`.

Superseded by the fuller, later re-measurement in `breadth-geometry-re-check-across-11-og-levels-2`, which explicitly notes this item's own numbers drifted (expected, from commits landing between the two measurements).
