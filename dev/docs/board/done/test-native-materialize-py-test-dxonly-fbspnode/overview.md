+++
priority = "p3"
kind = "debug"
summary = "Superseded, folded into `native-light-apply-bake-where-it-stands-and`. Pure duplicate of `test-dxonly-fbspnode-semantics-pinned-raises` (same bug: `IndexError` instead of a clean skip on a shallow checkout). See that item for the live version."
+++

# `test_native_materialize.py::test_dxonly_fbspnode_semantics_pinned` crashes with a bare `IndexError` instead of skipping, when the checkout sits shallow in the filesystem

Superseded, folded into `native-light-apply-bake-where-it-stands-and`.

Pure duplicate of `test-dxonly-fbspnode-semantics-pinned-raises` (same bug: `IndexError` instead of a clean skip on a shallow checkout). See that item for the live version.
