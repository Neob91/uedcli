+++
priority = "p3"
kind = "implement"
summary = "Add the missing surface poly-flags to the settable `--add-flag`/`--remove-flag` set (`PF_NAMES`)"
+++

# Add the missing surface poly-flags to the settable set

Done: appended `bigwavy`/`smallwavy`/`lowshadowdetail`/`brightcorners`/`highshadowdetail` to
`query.PF_NAMES`. Owner-gated `kb/textures.md` count edit parked in
`inbox/kb-textures-md-poly-flag-count-16-21-drop-the`.
