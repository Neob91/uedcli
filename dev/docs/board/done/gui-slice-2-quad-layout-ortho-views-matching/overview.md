+++
priority = "p1"
kind = "implement"
summary = "GUI Slice 2: quad layout, ortho views matching actor diagram, org panel, level picker"
+++

# GUI Slice 2: quad layout, ortho views matching actor diagram, org panel, level picker

Done. Quad layout, ortho views (CSG-colored, matching `actor diagram`), multi-select, org panel,
level picker, grid, theme, and 4-distinct shading modes all shipped. Ortho marquee-select was
scoped out — see `ortho-marquee-drag-select-rubber-band-multi`.
