+++
priority = "p2"
kind = "debug"
summary = "Superseded, folded into `native-light-apply-bake-where-it-stands-and`. SHIPPED — both the `PF_Invisible` zone-crossing gate fix and the `GetVisibleSurfs` DFS-order fix landed. Folded into `native-light-apply-bake-where-it-stands-and`'s status; full detail retained in git history and `dev/docs/native-materialize-findings.md`."
+++

# Zone-crossing `GetVisibleSurfs` gap: invisible portals were never crossed, now fixed

Superseded, folded into `native-light-apply-bake-where-it-stands-and`.

SHIPPED — both the `PF_Invisible` zone-crossing gate fix and the `GetVisibleSurfs` DFS-order fix landed. Folded into `native-light-apply-bake-where-it-stands-and`'s status; full detail retained in git history and `dev/docs/native-materialize-findings.md`.
