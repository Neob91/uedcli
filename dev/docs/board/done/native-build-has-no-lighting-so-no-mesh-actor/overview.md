+++
priority = "p2"
kind = "implement"
summary = "Superseded, folded into `native-light-apply-bake-where-it-stands-and`. The cited cause (no lighting) is long since fixed — lighting bakes offline (`native-light-apply-bake-where-it-stands-and`). The item's own remaining ask (re-check mesh actors draw in-game) was never re-run; not chased further, no longer a live blocker."
+++

# The editor-free build draws no mesh actors because it bakes no lighting

Superseded, folded into `native-light-apply-bake-where-it-stands-and`.

The cited cause (no lighting) is long since fixed — lighting bakes offline (`native-light-apply-bake-where-it-stands-and`). The item's own remaining ask (re-check mesh actors draw in-game) was never re-run; not chased further, no longer a live blocker.
