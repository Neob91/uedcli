+++
priority = "p?"
kind = "unknown"
summary = "`select_by_name` brush box sizing"
+++

# `select_by_name` brush box sizing

— brush targets drive a box sized to union world bounds
(+margin) for full containment. Verified end-to-end. See [[uned-brush-selectability]].
