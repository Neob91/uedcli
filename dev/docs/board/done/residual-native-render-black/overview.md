+++
priority = "p1"
kind = "debug"
summary = "Superseded, folded into `native-light-apply-bake-where-it-stands-and`. Old-wave (pre-2026-08-14) black-render note whose root-cause explanation (native writing `FBspSurf.iZone`) is now known WRONG — that field is `PanU`/`PanV`, not a zone pair (`dev-docs-states-fbspsurf-izone-where-the-field`). Superseded."
+++

# Residual native render-black (s76/s69/s07 left) is ZONE/SKY PORTALIZATION, not lighting/solidity — corrects the occlusion-fix handoff premise

Superseded, folded into `native-light-apply-bake-where-it-stands-and`.

Old-wave (pre-2026-08-14) black-render note whose root-cause explanation (native writing `FBspSurf.iZone`) is now known WRONG — that field is `PanU`/`PanV`, not a zone pair (`dev-docs-states-fbspsurf-izone-where-the-field`). Superseded.
