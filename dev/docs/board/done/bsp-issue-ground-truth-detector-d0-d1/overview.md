+++
priority = "p1"
kind = "implement"
summary = "BSP-issue ground-truth detector = D0 + D1 — superseded by `bsp-issue-detector` (shipped); folded into reconcile item"
+++

# BSP-issue ground-truth detector (D0+D1) — superseded

The D0/D1 detector spec was superseded by the shipped
`bsp-issue-detector` (in `done/`), which folds both into `level materialize` (build-output
drop-warning counts + built-model located defects, advisory at rc 0). Its open
re-scope questions live on `reconcile-the-bsp-issue-ground-truth-detector`. Full original spec:
`spec.md`.