+++
priority = "p2"
kind = "implement"
summary = "Built the audio arm (sound + music, phase a): enumeration, .umx title, refuse/--force store."
+++

# Audio arm (sound + music) — phase a — DONE 2026-08-02

Shipped both nouns: offline enumeration with the name-keyed identity/collision rule (`audioindex`),
the `.umx` embedded-title sniffer (`umxtitle`), the refuse-then-`--force` classification store
(`audio_catalog`), the shared `cli/commands/audio.py` behind `sound`/`music` parsers, and
`docs/usage.md`. Owner-gated `architecture.md` / `unrealed/` fold-backs parked in `board/inbox/`.
