+++
priority = "p3"
kind = "implement"
summary = "`classify prune` / `sync --prune` — CLOSED 2026-08-02, subsumed by the shard-based prune."
+++

# `classify prune` / `sync --prune` — closed

Owner ruled 2026-08-02: **fold/close.** Option A targeted the legacy name-keyed catalog, which the
asset-catalog redesign DELETES (not migrates). The shard-based `classify prune` ships with the class /
texture arms (see board item `class-classify-list-outdated-prune-not-built`). Building the
legacy-manifest version now is throwaway. Closed.
