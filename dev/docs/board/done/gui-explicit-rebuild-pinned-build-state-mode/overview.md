+++
priority = "p2"
kind = "implement"
summary = "GUI: explicit rebuild — pinned build state, mode gating, fullbright overlay for GUI edits"
+++

# GUI: explicit rebuild — pinned build state, mode gating, fullbright overlay for GUI edits

Sections 1-4 done: Load and Rebuild are independent, geometry never auto-builds. Section 5
(fullbright overlay for GUI-originated edits) remains deferred to P2, not started.
