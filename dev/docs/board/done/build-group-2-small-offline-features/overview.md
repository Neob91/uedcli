+++
priority = "p?"
kind = "implement"
summary = "Build group 2 — `actor bbox` and two more small offline model-side verbs"
+++

# Build group #2 — three small offline features

A 2026-07-18 build group sequencing three small, offline, model-side verbs (`actor bbox` and two
siblings) that needed no editor and were testable in CI.

This item exists to hold the plan, which no board entry owned.
