+++
priority = "p2"
kind = "debug"
summary = "Superseded, folded into `native-light-apply-bake-where-it-stands-and`. Claimed closing the UNATCO node gap to 0 (6314/6314) via a zone-pass reorder; superseded by later re-measurement showing UNATCO still +7 nodes over golden (`breadth-geometry-re-check-across-11-og-levels-2`) — the claimed fix's effect did not hold or was not the whole story. Current status: `native-light-apply-bake-where-it-stands-and`."
+++

# `csgRebuild` runs `TestVisibility` between the repartition and the detail-brush loop

Superseded, folded into `native-light-apply-bake-where-it-stands-and`.

Claimed closing the UNATCO node gap to 0 (6314/6314) via a zone-pass reorder; superseded by later re-measurement showing UNATCO still +7 nodes over golden (`breadth-geometry-re-check-across-11-og-levels-2`) — the claimed fix's effect did not hold or was not the whole story. Current status: `native-light-apply-bake-where-it-stands-and`.
