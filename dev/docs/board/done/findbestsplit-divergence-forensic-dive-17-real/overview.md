+++
priority = "p2"
kind = "debug"
summary = "Superseded, folded into `native-light-apply-bake-where-it-stands-and`. Its conclusion (root cause is `bspMergeCoplanars` fragment-shape drift) is superseded: later live capture exonerates `FindBestSplit`'s windowed-stride selection twice and re-attributes the divergence to a Pass-1 poly-order difference further upstream (`dev/docs/native-materialize-findings.md`, `freeclinic08-nsfhq04-1-surf-under-build-root`)."
+++

# FindBestSplit divergence forensic dive: 17 real tree-split points, root cause is bspMergeCoplanars fragment-shape drift, not FindBestSplit itself

Superseded, folded into `native-light-apply-bake-where-it-stands-and`.

Its conclusion (root cause is `bspMergeCoplanars` fragment-shape drift) is superseded: later live capture exonerates `FindBestSplit`'s windowed-stride selection twice and re-attributes the divergence to a Pass-1 poly-order difference further upstream (`dev/docs/native-materialize-findings.md`, `freeclinic08-nsfhq04-1-surf-under-build-root`).
