+++
priority = "p?"
kind = "implement"
summary = "expose order_value as a human-readable CSG rank (CLI verb + GUI inspector field)"
+++

# expose order_value as a human-readable CSG rank

Shipped: `actor rank` CLI verb (`docs/reference/actor/rank.md`) and `SceneActor.csg_rank`, shown in
the Inspector instead of the raw `order_value` string. `cb4d167b`.
