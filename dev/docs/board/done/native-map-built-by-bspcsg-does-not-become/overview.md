+++
priority = "p1"
kind = "debug"
summary = "Superseded, folded into `native-light-apply-bake-where-it-stands-and`. Old-wave (pre-2026-08-14) playability note. The native build path this describes (raw `bspcsg` output, no lighting/movers) was since superseded by `assemble_unbuilt` + `MAP LOAD` (`editor-free-native-world-bsp-map-assembly`)."
+++

# Native map built by `bspcsg` does not become PLAYABLE in-game (pawn never possesses / `--game` travel never completes) even though the shipped map travels fine in the same warm container

Superseded, folded into `native-light-apply-bake-where-it-stands-and`.

Old-wave (pre-2026-08-14) playability note. The native build path this describes (raw `bspcsg` output, no lighting/movers) was since superseded by `assemble_unbuilt` + `MAP LOAD` (`editor-free-native-world-bsp-map-assembly`).
