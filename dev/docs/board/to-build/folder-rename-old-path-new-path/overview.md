+++
priority = "p3"
kind = "implement"
summary = "`folder rename <old-path> <new-path>` — whole-subtree re-parent/rename"
+++

# `folder rename <old-path> <new-path>` — whole-subtree re-parent/rename

p3. Deferred
from the actor-folders v1 (assign-only). Re-parent/rename an entire folder subtree in one call
(rewrite the `folder` sidecar of every actor under `<old-path>` to the `<new-path>` prefix).
Andrzej, 2026-07-18.
