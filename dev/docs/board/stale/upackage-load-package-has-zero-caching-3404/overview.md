+++
priority = "p1"
kind = "implement"
summary = "upackage.load_package has zero caching -- 3404 reparse calls rendering one photo"
+++

# upackage.load_package has zero caching

Superseded by `unify-ue1-package-read-primitives-into-one-rust`, which bakes caching into the same
change that ports the read path to Rust.
