+++
priority = "p3"
kind = "debug"
summary = "`stash apply` double-suffixes names (`Pillar_iisch_77db4m`) and its `--at` anchor (bbox-min corner) is undocumented"
+++

# `stash apply` double-suffixes names (`Pillar_iisch_77db4m`) and its `--at` anchor (bbox-min corner) is undocumented

Cosmetic naming + a doc gap the agent had to probe empirically
(is `--at` centroid or bbox-min? → bbox-min). (Agent C.)
