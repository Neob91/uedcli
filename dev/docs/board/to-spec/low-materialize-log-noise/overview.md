+++
priority = "p3"
kind = "chore"
summary = "LOW: materialize log noise — `XGetWindowProperty failed (code=1)` repeated on every editor run"
+++

# LOW: materialize log noise — `XGetWindowProperty failed (code=1)` repeated on every editor run

p3. Cosmetic; buries the real message.
