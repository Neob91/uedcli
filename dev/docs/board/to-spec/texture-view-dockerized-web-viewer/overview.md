+++
priority = "p3"
kind = "implement"
summary = "`texture view` + dockerized web viewer with a search UI"
+++

# `texture view` + dockerized web viewer with a search UI

— reads the
tracked `texture-catalog/` + gitignored `.uedcli/textures/`; `view` is the entry verb. See
`decisions.md` 2026-06-22. Deferred from the 2026-06-22 texture tool.
