+++
priority = "p3"
kind = "implement"
summary = "`classify prune` / `sync --prune`"
+++

# `classify prune` / `sync --prune`

— explicitly remove `removed` entries
from the catalog manifest; v1 only marks them. Deferred from the 2026-06-22 texture tool.
