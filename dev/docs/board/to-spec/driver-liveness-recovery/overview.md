+++
priority = "p?"
kind = "implement"
summary = "Driver liveness recovery"
+++

# Driver liveness recovery

Fast crash detection is DONE (`wine_ctl`
`_assert_alive()`). Still TODO: higher-level *recovery* (restart + resume) around brush writes —
see [[uned-liveness-monitoring]].
