+++
priority = "p?"
kind = "implement"
summary = "Zones"
+++

# Zones

— water / fog / gravity / ambient light / reverb / sound regions. A
zone portal is a *surface flag* (supported by `poly set`); zones resolve on rebuild; max 64.
Place/configure `ZoneInfo` actors + flag the portal surfaces.
