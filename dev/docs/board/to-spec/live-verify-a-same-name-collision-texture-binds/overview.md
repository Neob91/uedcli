+++
priority = "p2"
kind = "debug"
summary = "Live-verify a same-name-collision texture binds correctly end-to-end through a real `apply`"
+++

# Live-verify a same-name-collision texture binds correctly end-to-end through a real `apply`

— not just the synthetic `MAP IMPORTADD` probe (`unrealed/quirks.md` "T3D
format") that found the auto-demand-load bug `apply._ensure_load` already fixes via an explicit
`OBJ LOAD` per package.
