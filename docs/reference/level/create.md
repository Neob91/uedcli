# level create

`level create <name>` — scaffold a NEW level directory `maps/<name>/` with a `LevelInfo` actor
(required by `materialize`); prints `to edit it: export UEDCLI_LEVEL=<name>`.

See also: [`level list`](list.md), [`level status`](status.md).
