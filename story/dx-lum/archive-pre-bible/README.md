# Archived: pre-bible (off-canon) first run

These 5 stories, 6 analyses, and summary were produced BEFORE the real DX_LUM story bible was
located (it lives in a separate repo, `dx_lum`, under `DevDocs/Story/`). They were seeded from a
guess that DX_LUM was a "light/surveillance" game — that guess was wrong. They contradict canon
(no FMA, no multiverse energy-siphoning, no Ryker/Quinmore). Kept only as raw creative output.

The canon-grounded run is one level up in `raw-stories/`, `findings/`, `SUMMARY.md`.
